apiVersion: v1
kind: Service
metadata:
  name: billing-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
