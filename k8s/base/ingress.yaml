apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-service
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: billing-service
                port:
                  name: http
